import { styled } from "@mui/material";

export const PostDetailComment = styled('div')(({theme}) => ({
    marginTop: theme.spacing(1),
    background:'#fff',
    borderRadius:'10px',
    padding: theme.spacing(2)
}))