import {styled} from '@mui/material/styles';

export const NotificationItemWap = styled('div')(({theme}) => ({
    padding: '5px 10px',
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
}));